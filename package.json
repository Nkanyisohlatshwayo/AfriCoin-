Your repository has been created successfully. You can see it at github.com/Nkanyisohlatshwayo/AfriCoin-.
Now tap "creating a new file" (the blue link near the bottom of the screen). This will open GitHub's file editor where you will create your first file.
First file — package.json
Type package.json as the filename and paste the following as the content:
{
  "name": "africoin",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.4.0"
  }
}
